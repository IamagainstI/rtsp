[package]
name = "media"
version = "0.1.0"
edition = "2021"

[dependencies]
abstractions = { version = "0.1.0", path = "../abstraction" }
base64 = "0.22.1"
hex.workspace = true
rstest.workspace = true
